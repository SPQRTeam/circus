width: 9.0                  # x dimension (long side length of the field)
height: 4.7                 # y dimension (short side length of the field)
center_radius: 1.0         # center circle radius
goal_area_width: 0.8        # goal area width (short side length of the goal area)
goal_area_height: 2.76       # goal area height (long side length of the goal area)
penalty_area_width: 2.0    # penalty area width (short side length of the penalty area)
penalty_area_height: 3.5    # penalty area height (long side length of the penalty area)
penalty_mark_distance: 1.5  # distance of penalty mark from goal line
goal_width: 2.6             # goal width
goal_height: 1.8            # goal height
goal_depth: 0.6             # goal depth
line_width: 0.08            # line thickness
ball_radius: 0.11           # ball radius
